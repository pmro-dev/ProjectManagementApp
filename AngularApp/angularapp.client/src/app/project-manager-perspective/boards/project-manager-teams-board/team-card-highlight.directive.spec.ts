import { TeamCardHighlightDirective } from './team-card-highlight.directive';

describe('TeamCardHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new TeamCardHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
