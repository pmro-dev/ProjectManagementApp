import { TodolistCardColorDirective } from './todolist-card-color.directive';

describe('TodolistCardColorDirective', () => {
  it('should create an instance', () => {
    const directive = new TodolistCardColorDirective();
    expect(directive).toBeTruthy();
  });
});
